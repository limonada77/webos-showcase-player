name: StreamTV Admin Android

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "android-admin/**"
      - ".github/workflows/android-admin.yml"

jobs:
  apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Android SDK
        uses: android-actions/setup-android@v3

      - name: Instalar Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.9"

      - name: Gerar APK Admin
        working-directory: android-admin
        run: gradle assembleRelease --no-daemon --stacktrace

      - name: Preparar APK
        run: |
          mkdir -p apk-out
          cp android-admin/app/build/outputs/apk/release/app-release.apk apk-out/StreamTV-Admin.apk
          ls -lh apk-out
          sha256sum apk-out/StreamTV-Admin.apk

      - name: Enviar APK
        uses: actions/upload-artifact@v4
        with:
          name: StreamTV-Admin-Android
          path: apk-out/StreamTV-Admin.apk
          if-no-files-found: error
